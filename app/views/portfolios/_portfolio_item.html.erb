  <div class="card">
    <%= image_tag portfolio_item.thumg_image unless portfolio_item.thumg_image.nil? %>
    <p class="card-text">
      <span><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></span> <%= portfolio_item.subtitle %>
    </p>
  </div>
  
 
 
  